# Skill: gic-integration

Builds GiC cartridges for an app: queries the source notebook for per-tab deep-layer commentary, writes the cartridge prose following the control plane format (authored context statement → app description → interaction mode → per-tab knowledge → state line), and saves it to the app's cartridges/ folder.

**Actual skill:** `~/.claude/skills/gic-integration/` — implemented.
