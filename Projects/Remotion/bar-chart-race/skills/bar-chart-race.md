# Skill: bar-chart-race

Creates a new bar chart race data project and renders it: write `data/{project}/index.ts` → stage → activate → preview → render → complete.

Also covers: data schema (`BarChartConfig`), story cards, music, timing parameters, folder conventions.

**Actual skill:** `~/.claude/skills/bar-chart-race/` — not yet implemented.
