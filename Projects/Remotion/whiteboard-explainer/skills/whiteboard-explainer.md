# Skill: whiteboard-explainer

Adds new scene types, themes, and compositions to the whiteboard explainer system: update types.ts → build scene component → register in SceneDispatcher → create scenes JSON → add composition to Root.tsx.

Also covers: theme system, naming conventions, gotchas (TransitionSeries, composition IDs).

**Actual skill:** `~/.claude/skills/whiteboard-explainer/` — not yet implemented.
