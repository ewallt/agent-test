Read the slide manifest from the source I've identified in this message. If it defines every slide's title, body text, and image prompt, as well as the total slide count, generate exactly the number of slides it specifies and follow its instructions. Otherwise ask the User for details.

Build a single-file HTML slideshow from that manifest. Use the reference implementation below as your exact technical blueprint — copy its structure, CSS values, and code patterns precisely:

Layout: 16/9 aspect ratio, 50/50 grid, CSS values as shown — do not alter font sizes or proportions
Images: <span class="loader"></span> only in the un-baked state — the script creates <img> tags dynamically; do not pre-populate <img> tags
Loading: use loadImagesSequentially to prevent API throttling
Copy mechanism: implement copyPart(1) and copyPart(2) exactly as shown — do not replace with a single copy button
Environment: do not hardcode Firebase, getUserMedia, or fetch override scripts in <head> — the environment injects these at runtime
<!DOCTYPE html><html lang="en"><head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>Dynamic Presentation</title> <link rel="preconnect" href="https://fonts.googleapis.com"> <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin=""> <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500&family=Playfair+Display:ital,wght@0,600;1,600&display=swap" rel="stylesheet"> <style> body { margin: 0; padding: 0; font-family: 'Inter', sans-serif; background-color: #020617; } .slideshow-container { position: relative; width: 100vw; height: 100vh; background: #020617; overflow: hidden; display: flex; align-items: center; justify-content: center; } .slide { position: absolute; opacity: 0; transition: opacity 0.5s ease; pointer-events: none; width: 100%; display: flex; justify-content: center; } .slide.active { opacity: 1; pointer-events: auto; z-index: 10; } .nav-button { position: absolute; top: 50%; transform: translateY(-50%); z-index: 20; color: white; font-size: 2rem; background: rgba(0,0,0,0.5); border: none; padding: 1rem; cursor: pointer; transition: background 0.3s ease; border-radius: 8px; } .nav-button:hover { background: rgba(0,0,0,0.8); } .nav-button.prev { left: 20px; } .nav-button.next { right: 20px; } .slide-card { width: 95%; max-width: 1200px; aspect-ratio: 16/9; display: grid; grid-template-columns: 1fr 1fr; background: #0f172a; border-radius: 12px; overflow: hidden; container-type: inline-size; margin: 0 auto; box-shadow: 0 25px 50px -12px rgba(0,0,0,0.7); } .text-column { display: flex; flex-direction: column; justify-content: center; padding: 5cqw; box-sizing: border-box; } .text-column h2 { font-size: 3.2cqw; color: #f8fafc; font-family: 'Playfair Display', serif; margin-top: 0; margin-bottom: 1.5cqw; line-height: 1.1; } .text-column p { font-size: 1.4cqw; color: #cbd5e1; font-family: 'Inter', sans-serif; line-height: 1.5; margin-top: 0; margin-bottom: 1.5cqw; } .text-column p:last-child { margin-bottom: 0; } .image-column { width: 100%; height: 100%; background-color: #0b1121; display: flex; align-items: center; justify-content: center; position: relative; } .image-column img { width: 100%; height: 100%; object-fit: contain; object-position: center; filter: sepia(0.8) contrast(1.2) brightness(0.9); display: block; opacity: 0; transition: opacity 0.5s ease-in; position: absolute; top: 0; left: 0; } .image-column img.loaded { opacity: 1; } .loader { width: 48px; height: 48px; border: 5px solid #cbd5e1; border-bottom-color: transparent; border-radius: 50%; display: inline-block; box-sizing: border-box; animation: rotation 1s linear infinite; } .error-msg { color: #ef4444; font-size: 1.2cqw; text-align: center; padding: 1rem; max-width: 80%; } @keyframes rotation { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } } .controls-container { position: fixed; bottom: 20px; right: 20px; z-index: 100; display: flex; gap: 12px; } .save-btn { background: #cbd5e1; color: #020617; border: none; padding: 10px 20px; border-radius: 8px; font-family: 'Inter', sans-serif; font-weight: 500; font-size: 1rem; cursor: pointer; box-shadow: 0 4px 6px rgba(0,0,0,0.3); transition: background 0.2s, transform 0.2s; } .save-btn:hover { background: #f8fafc; transform: scale(1.05); } </style></head><body> <main class="slideshow-container"> <button class="nav-button prev" onclick="changeSlide(-1)">❮</button> <button class="nav-button next" onclick="changeSlide(1)">❯</button> <div class="controls-container"><button class="save-btn" id="btn-part1" onclick="copyPart(1)">📋 Copy Part 1</button><button class="save-btn" id="btn-part2" onclick="copyPart(2)">📋 Copy Part 2</button></div> <!-- Generate the requested number of section.slide elements based on the manifest --> <section class="slide active"> <div class="slide-card"> <div class="text-column"> <h2>Slide Title Here</h2> <p>First paragraph goes here.</p> <p>Second paragraph goes here.</p> </div> <div class="image-column" id="img-container-0"> <span class="loader"></span> </div> </div> </section> </main> <script> let currentSlideIndex = 0; const slides = document.querySelectorAll('.slide'); function showSlide(index) { slides.forEach(slide => slide.classList.remove('active')); if (index >= slides.length) currentSlideIndex = 0; else if (index < 0) currentSlideIndex = slides.length - 1; else currentSlideIndex = index; slides[currentSlideIndex].classList.add('active'); } function changeSlide(direction) { showSlide(currentSlideIndex + direction); } document.addEventListener('keydown', (event) => { if (event.key === 'ArrowLeft') changeSlide(-1); else if (event.key === 'ArrowRight') changeSlide(1); }); function copyPart(partNumber) { if (document.querySelectorAll('.loader').length > 0) { const targetBtn = document.getElementById(`btn-part${partNumber}`); const originalText = targetBtn.innerText; targetBtn.innerText = "⏳ Awaiting images..."; setTimeout(() => targetBtn.innerText = originalText, 2500); return; } const container = document.querySelector('.controls-container'); container.style.display = 'none'; const htmlContent = "<!DOCTYPE html>\n" + document.documentElement.outerHTML; container.style.display = 'flex'; const midpoint = Math.floor(htmlContent.length / 2); let splitIndex = htmlContent.indexOf('<section class="slide"', midpoint); if (splitIndex === -1) splitIndex = midpoint; const contentToCopy = partNumber === 1 ? htmlContent.substring(0, splitIndex) : htmlContent.substring(splitIndex); const targetBtn = document.getElementById(`btn-part${partNumber}`); const originalText = targetBtn.innerText; const textarea = document.createElement('textarea'); textarea.value = contentToCopy; textarea.style.position = 'fixed'; textarea.style.left = '-9999px'; document.body.appendChild(textarea); textarea.select(); try { const successful = document.execCommand('copy'); targetBtn.innerText = successful ? "✅ Copied!" : "❌ Failed"; } catch (err) { targetBtn.innerText = "❌ Failed"; console.error("Clipboard write failed:", err); } document.body.removeChild(textarea); setTimeout(() => targetBtn.innerText = originalText, 4000); } const apiKey = ""; const imagePrompts = [ // Populated from manifest "Prompt 1", "Prompt 2" ]; async function fetchImageWithRetry(prompt, containerId) { const container = document.getElementById(containerId); if (!container) return; const delays = [1000, 2000, 4000, 8000, 16000]; for (let i = 0; i < 5; i++) { try { const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/imagen-4.0-generate-001:predict?key=${apiKey}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ instances: [{ prompt: prompt }], parameters: { sampleCount: 1 } }) }); if (!response.ok) throw new Error('API request failed'); const result = await response.json(); const base64 = result.predictions?.[0]?.bytesBase64Encoded; if (base64) { const img = document.createElement('img'); img.src = `data:image/png;base64,${base64}`; img.alt = "Generated Presentation Image"; img.onload = () => { img.classList.add('loaded'); const loader = container.querySelector('.loader'); if (loader) loader.remove(); }; container.appendChild(img); return; } throw new Error('Invalid image data format'); } catch (err) { if (i === 4) { const loader = container.querySelector('.loader'); if (loader) loader.remove(); container.innerHTML += `<div class="error-msg">Generation Unavailable<br><small>Service limit or connection error.</small></div>`; } else { await new Promise(resolve => setTimeout(resolve, delays[i])); } } } } async function loadImagesSequentially() { for (let index = 0; index < imagePrompts.length; index++) { await fetchImageWithRetry(imagePrompts[index], `img-container-${index}`); } } window.addEventListener('DOMContentLoaded', () => { if (document.querySelectorAll('.image-column img').length > 0) { return; } loadImagesSequentially(); }); </script></body></html>