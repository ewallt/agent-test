# Skill: byg-illustration

Builds a BYG illustration video end-to-end: script → TSX file → register in Root.tsx → preview → render → wrapper HTML → deploy to gh-pages → update web app → update status table.

Also contains: naming conventions (TSX, wrapper HTML, MP4, gh-pages subfolder, illustration folder) and folder structure.

**Actual skill:** `~/.claude/skills/byg-illustration/` — not yet implemented.
